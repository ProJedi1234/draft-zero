/**
 * The app's micro-label register, in one place.
 *
 * Same values as components/ui/badge.tsx, which is where the register was set:
 * 10px, semibold, wide tracking, uppercase. The cost surfaces had two spellings
 * of it — the ledger's headings and the usage page's figure captions, one of
 * them a size up and without the weight — which made one feature read as two.
 */
export const MICRO_LABEL =
  "text-[0.625rem] font-semibold tracking-widest uppercase text-muted-foreground"
